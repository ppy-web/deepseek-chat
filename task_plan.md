# 语音能力迁移计划

## 目标
- 从 `E:\project\xiaomei\apps\pc` 迁移语音输入和文字转语音交互到当前 Vue 3 项目。
- 真实后端接口先用占位实现，后续可以替换为正式 STT/TTS 服务。

## 阶段
- [x] 梳理当前输入框、消息操作栏和接口层。
- [x] 梳理 `xiaomei/apps/pc` 的语音输入与朗读实现。
- [x] 新增当前项目内的语音输入与 TTS composable/service。
- [x] 接入输入框语音按钮与消息朗读按钮。
- [x] 运行 typecheck/build 并修复问题。

## 约束
- 只修改语音迁移相关文件。
- 不回滚已有用户改动，不提交或推送 Git。
